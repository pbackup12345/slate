import { Editor, NodeEntry, NodeMatch } from '../..';
declare class NodeQueries {
    /**
     * Check if a node entry is a match.
     */
    isNodeMatch(this: Editor, entry: NodeEntry, match: NodeMatch | NodeMatch[]): boolean;
}
export default NodeQueries;
//# sourceMappingURL=node.d.ts.map